# 代码优化迁移检查表

## ? 现状分析

### 原始代码问题

| 问题 | 位置 | 影响 | 优化方案 |
|------|------|------|---------|
| 50+ 行 extern 声明 | bsp_ma_su.c L1-150 | 代码混乱，难以维护 | 使用数组 + system_data.h |
| 18 行重复 PID 参数 | main.c L26-79 | 冗余，不易修改 | motor_pid_params.h 表驱动 |
| 主循环 120+ 行 | main.c L165-274 | 逻辑复杂，难以理解 | control_task 模块 |
| 4 个相同的足端计算 | main.c L192-212 | 重复代码，难以维护 | 循环 + 数组索引 |
| 全局变量混乱 | main.c + bsp_ma_su.c | 难以跟踪状态变化 | 状态结构体 (dog_state_t) |
| 无统一的初始化 | main.c L125-155 | 初始化流程不清晰 | _system_init() 函数 |

---

## ? 优化检查清单

### 第一阶段：创建新模块

- [ ] **创建 control_task 模块**
  - [ ] 创建 `User/control_task/` 目录
  - [ ] 创建 `control_task.h`（定义 dog_state_t）
  - [ ] 创建 `control_task.c`（实现控制逻辑）
  - [ ] 添加 `control_task_timer_handler()` 函数
  - [ ] 添加 `control_task_execute()` 函数
  - [ ] 编译测试通过

- [ ] **创建 PID 参数管理**
  - [ ] 创建 `motor_pid_params.h`
  - [ ] 整理所有 9 个电机的速度 PID 参数
  - [ ] 整理所有 9 个电机的位置 PID 参数
  - [ ] 添加限幅参数
  - [ ] 提供访问宏

- [ ] **创建数据结构头**
  - [ ] 创建 `system_data.h`
  - [ ] 定义全局数据结构（数组）
  - [ ] 提供访问器函数
  - [ ] 定义初始化函数

### 第二阶段：更新核心文件

- [ ] **更新 main.c**
  - [ ] 替换初始化为 `_system_init()`
  - [ ] 简化主循环为单一 `control_task_execute()` 调用
  - [ ] 删除所有 extern 电机声明
  - [ ] 删除重复的 PID 参数定义
  - [ ] 删除足端轨迹全局变量
  - [ ] 删除关节角度全局变量

- [ ] **更新 stm32f4xx_it.c**
  - [ ] GENERAL_TIM_IRQHandler 调用 `control_task_timer_handler()`
  - [ ] 删除所有中断内的计数和标志设置
  - [ ] 保持中断处理时间 < 0.5ms

- [ ] **重构 bsp_ma_su.c**
  - [ ] 删除 50+ 行 extern 声明
  - [ ] 改用 system_data.h 中的数组
  - [ ] 使用 motor_pid_params.h 中的参数表
  - [ ] 提供统一的初始化函数
  - [ ] 优化 PID 初始化循环

### 第三阶段：验证和测试

- [ ] **编译验证**
  - [ ] CMakeLists.txt 添加新模块
  - [ ] 整个项目编译通过（无错误）
  - [ ] 整个项目编译通过（无警告）

- [ ] **功能测试**
  - [ ] 电机通信正常
  - [ ] RC 遥控正常
  - [ ] 步态计算正确
  - [ ] PID 控制有效
  - [ ] 基本运动可执行

- [ ] **性能测试**
  - [ ] 中断时间 < 0.5ms（可用示波器或计时器测试）
  - [ ] 主循环响应时间 < 10ms
  - [ ] 无明显延迟或卡顿

- [ ] **代码质量**
  - [ ] 代码行数减少 > 50%
  - [ ] extern 声明减少 > 80%
  - [ ] 圈复杂度降低
  - [ ] 代码注释完整

---

## ? 文件对标表

### 需要替换或删除的代码

| 原文件 | 行数 | 优化后 | 行数 | 削减 |
|--------|------|--------|------|------|
| main.c | 274 | main_optimized.c | 120 | 56% |
| bsp_ma_su.c (头部) | 150 | 简化 | 50 | 67% |
| stm32f4xx_it.c (TIM) | 50 | 1 行调用 | 1 | 98% |
| 全局 PID 参数 | 18 行 | motor_pid_params.h 表 | 10 行 | 44% |

### 新增文件

| 文件 | 目的 | 行数 |
|------|------|------|
| control_task.h | 定义控制任务接口 | 80 |
| control_task.c | 实现控制逻辑 | 200 |
| motor_pid_params.h | 集中 PID 参数 | 120 |
| system_data.h | 统一数据结构 | 100 |
| system_data.c | 数据结构实现 | 80 |

---

## ? 代码迁移步骤

### 步骤 1: 模块开发（1-2 小时）

```bash
# 创建新目录
mkdir User/control_task

# 编写新文件
# - control_task.h
# - control_task.c
# - motor_pid_params.h
# - system_data.h
# - system_data.c
```

### 步骤 2: 文件更新（1-2 小时）

```bash
# 备份原始文件
cp User/main.c User/main_backup.c
cp User/stm32f4xx_it.c User/stm32f4xx_it_backup.c
cp User/bsp_ma_su/bsp_ma_su.c User/bsp_ma_su/bsp_ma_su_backup.c

# 使用优化版本替换核心文件
# 或手动迁移关键部分
```

### 步骤 3: 编译测试（30 分钟）

```bash
# Keil MDK 中：
# 1. 添加新文件到项目
# 2. Build All
# 3. 检查错误和警告
```

### 步骤 4: 功能测试（1-2 小时）

```bash
# 1. Download + Debug to STM32
# 2. 测试基本功能
# 3. 使用示波器/日志检查中断时间
# 4. 运动测试
```

---

## ?? 注意事项

### 避免的常见错误

| 错误 | 症状 | 解决方案 |
|------|------|---------|
| 忘记初始化新变量 | 电机不动或崩溃 | 调用 `system_data_init()` |
| PID 参数索引错误 | 某个电机控制不当 | 检查 motor_pid_params.h 索引 |
| 中断时间过长 | 系统不稳定 | 检查是否有浮点运算或 printf |
| extern 声明遗漏 | 编译错误 | 检查是否包含 system_data.h |
| 数组越界 | 崩溃 | 检查 motor_id 范围 (1-9) |

### 兼容性考虑

- ? 新代码与原始代码可共存（使用 .c 和 _optimized.c）
- ? 可分步迁移（先迁移中断，再迁移主循环）
- ? 原始文件保留（便于对比和回滚）

---

## ? 验证标准

### 编译阶段

```
? 无编译错误
? 警告 < 5 条
? 所有函数都被正确声明
```

### 功能阶段

```
? 电机初始化成功
? RC 遥控响应正常
? 步态计算输出合理
? PID 控制有效（可通过波形观察）
? 基本行走/跳跃动作可执行
```

### 性能阶段

```
? 中断时间: < 0.5ms
? 主循环时间: < 10ms
? 无系统崩溃或死锁
? 内存使用正常
```

---

## ? 优化前后对比

### 代码量

```
原始:
├─ main.c: 274 行
├─ bsp_ma_su.c: 1375 行 (含 150 行 extern)
├─ stm32f4xx_it.c: 50 行中断代码
└─ 全局变量: ~80+ 个

优化后:
├─ main_optimized.c: 120 行
├─ bsp_ma_su_optimized.h: 80 行 (仅接口)
├─ control_task.c: 200 行 (集中逻辑)
├─ stm32f4xx_it_optimized.c: 1 行中断代码
├─ motor_pid_params.h: 120 行 (参数表)
├─ system_data.h: 100 行 (数据结构)
└─ 全局变量: ~20 个 (结构体化)
```

### 可维护性

| 指标 | 原始 | 优化后 | 改进 |
|------|------|--------|------|
| 全局 extern | 50+ | 5 | 90% ↓ |
| 重复代码 | 多 | 无 | - |
| 代码复用 | 低 | 高 | + |
| 圈复杂度 | 高 | 低 | - |
| 单元测试友好 | 否 | 是 | ? |

---

## ? 优化完成确认

完成优化后，请确认：

- [ ] 所有文件都已创建或更新
- [ ] 编译无错误无警告
- [ ] 所有功能都能正常工作
- [ ] 中断时间 < 0.5ms
- [ ] 代码注释完整
- [ ] 单元测试通过（如有）
- [ ] 文档已更新
- [ ] 原始代码已备份

**优化完成日期**: ___________  
**负责人**: ___________  
**验收人**: ___________

---

**参考**: 见 `OPTIMIZATION_GUIDE.md` 获取详细信息
